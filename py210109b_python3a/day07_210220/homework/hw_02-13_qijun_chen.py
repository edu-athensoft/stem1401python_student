"""
copied source code from slack on Feb 20th, 2021

obvious error, not tested
"""

"""
from tkinter import *
from PIL import Image, ImageTk
root = Tk()
root.title('Python GUI - Homework 4')
def center(screen_length, screen_width, window_length, window_width):
    x = screen_length / 2
    y = screen_width / 2
    half-length = window_l / 2
    half-width = window_w / 2
    root.geometry(f"{window_l}x{window_w}"
                  f"+{int(x - half-length)}+{int(y - half-width)}")
center(1920, 1080, 800, 450)
root.maxsize(2000, 1000)
root.minsize(200, 100)
root.configure(bg='#aabbcc')
"""